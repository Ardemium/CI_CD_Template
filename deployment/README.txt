This folder contains all the necessary files for deploying the application,
including the configuration files for different environments, and any scripts or tools required for deployment.

Contains the Docker Compose file and any necessary configuration files for the nginx reverse proxy,
as well as scripts or tools for deploying the application to different environments.
Additionally, it could contain the Kubernetes configuration files that would help in deploying the services in a k8s cluster.